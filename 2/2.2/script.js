let hrs = Number(prompt("How much hours to convert to seconds?")),
  result = hrs * 3600;

if (hrs === 1) {
  alert(`${hrs} hour in seconds equals ${result}`);
} else {
  alert(`${hrs} hours in seconds equals ${result}`);
}
